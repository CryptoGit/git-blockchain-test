jobs:
  build:
    docker:
      - image: circleci/ruby:latest
    steps:
      - checkout
      - run:
          name: Verifying Block SHA against difficulty factor
          command: ruby .circleci/sha_verify.rb
